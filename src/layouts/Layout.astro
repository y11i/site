---
import Footer from '../components/Footer.astro';
import ShaderBackground from '../components/ShaderBackground.astro';

interface Props {
	title?: string;
	description?: string;
	noIndex?: boolean;
}

const { title, description, noIndex = false } = Astro.props;
const baseTitle = 'Y.S. Choi';
const fullTitle = title ? `${title} - ${baseTitle}` : `${baseTitle}`;
const metaDescription =
	description ??
	'Personal site of Sean Choi, featuring work, photography, and current updates.';
const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const ogImageURL = new URL('/og.jpg', Astro.site);
const personSchema = {
	'@context': 'https://schema.org',
	'@type': 'Person',
	name: 'Sean Choi',
	alternateName: 'Yeonseung Choi',
	url: 'https://www.ysc.dev',
	image: ogImageURL.href,
	jobTitle: 'Software Engineer',
	worksFor: { '@type': 'Organization', name: 'Valon' },
	sameAs: [
		'https://www.linkedin.com/in/szean',
		'https://github.com/y11i',
		'https://www.youtube.com/@szeanchoi',
		'https://www.instagram.com/szeanchoi',
		'https://x.com/yscdev'
	]
};
const websiteSchema = {
	'@context': 'https://schema.org',
	'@type': 'WebSite',
	name: 'Sean Choi',
	alternateName: baseTitle,
	url: 'https://www.ysc.dev'
};
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/png" href="/favicon-96x96.png" sizes="96x96" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="shortcut icon" href="/favicon.ico" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <meta name="generator" content={Astro.generator} />
    <link rel="manifest" href="/site.webmanifest" />
    <script is:inline>
      (() => {
        if (typeof window === 'undefined') return;

        const reducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

        if (!reducedMotion) {
          document.documentElement.classList.add('motion-enabled');
        }
      })();
    </script>
    <title>{fullTitle}</title>
    <meta name="description" content={metaDescription} />
    {noIndex && <meta name="robots" content="noindex, nofollow" />}
    <link rel="canonical" href={canonicalURL.href} />
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="Sean Choi" />
    <meta property="og:title" content={fullTitle} />
    <meta property="og:description" content={metaDescription} />
    <meta property="og:url" content={canonicalURL.href} />
    <meta property="og:image" content={ogImageURL.href} />
    <meta property="og:locale" content="en_US" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={fullTitle} />
    <meta name="twitter:description" content={metaDescription} />
    <meta name="twitter:image" content={ogImageURL.href} />
    <script type="application/ld+json" set:html={JSON.stringify(personSchema)} />
    <script type="application/ld+json" set:html={JSON.stringify(websiteSchema)} />
  </head>
  <body>
    <ShaderBackground />
    <main class="main-content">
      <slot />
    </main>
    <Footer />
    <script is:inline>
      (() => {
        if (typeof window === 'undefined') return;

        const stateKey = '__yscMotionState';
        const eventsKey = '__yscMotionEventsBound';

        function revealAllMotionElements() {
          document.documentElement.classList.remove('motion-enabled');
          document.querySelectorAll('[data-motion]').forEach((element) => {
            if (element instanceof HTMLElement) {
              element.classList.add('is-inview');
              element.dataset.motionReady = 'true';
            }
          });
        }

        function setupMotionSystem() {
          const reducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
          const previousState = window[stateKey];

          previousState?.observer?.disconnect();
          previousState?.mutationObserver?.disconnect();

          if (reducedMotion) {
            revealAllMotionElements();
            window[stateKey] = null;
            return;
          }

          document.documentElement.classList.add('motion-enabled');

          document.querySelectorAll('[data-motion]').forEach((element) => {
            if (!(element instanceof HTMLElement)) return;
            const shouldRepeat = element.dataset.motionRepeat !== 'false';
            const alreadyRevealed = element.classList.contains('is-inview');

            if (!shouldRepeat && alreadyRevealed) {
              element.dataset.motionReady = 'true';
              return;
            }

            delete element.dataset.motionReady;

            if (shouldRepeat) {
              element.classList.remove('is-inview');
            }
          });

          const observer = new IntersectionObserver(
            (entries) => {
              entries.forEach((entry) => {
                const element = entry.target;

                if (!(element instanceof HTMLElement)) return;

                const shouldRepeat = element.dataset.motionRepeat !== 'false';

                if (entry.isIntersecting) {
                  revealElement(element, shouldRepeat);

                  return;
                }

                if (shouldRepeat) {
                  element.classList.remove('is-inview');
                }
              });
            },
            {
              threshold: 0.18,
              rootMargin: '0px 0px -10% 0px',
            }
          );

          function revealElement(element, shouldRepeat) {
            if (element.dataset.motion === 'image') {
              element.classList.add('is-inview');
              element.dataset.motionLoaded = 'true';

              if (!shouldRepeat) {
                observer.unobserve(element);
              }

              return;
            }

            element.classList.add('is-inview');

            if (!shouldRepeat) {
              observer.unobserve(element);
            }
          }

          function registerElement(element) {
            if (!(element instanceof HTMLElement)) return;
            if (element.dataset.motionReady === 'true') return;
            if (element.dataset.motionRepeat === 'false' && element.classList.contains('is-inview')) {
              element.dataset.motionReady = 'true';
              return;
            }

            element.dataset.motionReady = 'true';
            element.classList.remove('is-inview');
            observer.observe(element);
          }

          function scan(node) {
            if (!node) return;

            if (node instanceof HTMLElement && node.matches('[data-motion]')) {
              registerElement(node);
            }

            if ('querySelectorAll' in node) {
              node.querySelectorAll('[data-motion]').forEach(registerElement);
            }
          }

          scan(document);

          const mutationObserver = new MutationObserver((mutations) => {
            mutations.forEach((mutation) => {
              mutation.addedNodes.forEach((node) => {
                scan(node);
              });
            });
          });

          if (document.body) {
            mutationObserver.observe(document.body, {
              childList: true,
              subtree: true,
            });
          }

          window[stateKey] = { observer, mutationObserver };
        }

        if (!window[eventsKey]) {
          window[eventsKey] = true;
          window.addEventListener('DOMContentLoaded', setupMotionSystem, { once: true });
          document.addEventListener('astro:page-load', setupMotionSystem);
          document.addEventListener('astro:after-swap', () => {
            requestAnimationFrame(setupMotionSystem);
          });
        }

        requestAnimationFrame(setupMotionSystem);
      })();
    </script>
  </body>
</html>

<style>
  :root {
    --motion-ease: cubic-bezier(0.22, 1, 0.36, 1);
    --motion-enter-duration: 750ms;
    --motion-enter-duration-fast: 550ms;
    --motion-exit-duration: 920ms;
    --motion-exit-duration-fast: 680ms;
  }

  html,
  body {
    font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
    margin: 0;
    width: 100%;
    min-height: 100%;
    color: #333;
    background-color: #fff;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }

  html {
    box-sizing: border-box;
    scroll-behavior: smooth;
    overflow-x: clip;
  }

  *,
  *::before,
  *::after {
    box-sizing: inherit;
  }

  body {
    position: relative;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    overflow-x: hidden;
    background-image: 
      radial-gradient(circle at 1px 1px, rgba(0, 0, 0, .065) 1.5px, transparent 0);
    background-size: 29px 29px;
  }

  :global(.shader-background-active body) {
    background-image: none;
  }

  .main-content {
    position: relative;
    z-index: 1;
    flex: 1;
    width: 100%;
    min-width: 0;
  }

  :global(body > footer) {
    position: relative;
    z-index: 1;
  }

  :global(.motion-enabled [data-motion]) {
    opacity: 0;
    transform: var(--motion-transform-hidden, translate3d(0, var(--motion-y, 18px), 0) scale(var(--motion-scale, 1)));
    filter: blur(var(--motion-blur, 5px));
    transition:
      opacity var(--motion-exit-duration, 920ms) var(--motion-ease),
      transform var(--motion-exit-duration, 920ms) var(--motion-ease),
      filter var(--motion-exit-duration-fast, 680ms) ease;
    transition-delay: 0ms;
    will-change: opacity, transform, filter;
  }

  :global(.motion-enabled [data-motion='text']) {
    --motion-y: 15px;
    --motion-blur: 5px;
  }

  :global(.motion-enabled [data-motion='media']) {
    --motion-y: 10px;
    --motion-scale: 0.985;
    --motion-blur: 0px;
  }

  :global(.motion-enabled [data-motion='image']) {
    --motion-blur: 0px;
    transition:
      opacity 1150ms ease;
  }

  :global(.motion-enabled [data-motion].is-inview) {
    opacity: 1;
    transform: var(--motion-transform-visible, translate3d(0, 0, 0) scale(1));
    filter: blur(0);
    transition:
      opacity var(--motion-enter-duration, 640ms) var(--motion-ease),
      transform var(--motion-enter-duration, 640ms) var(--motion-ease),
      filter var(--motion-enter-duration-fast, 440ms) ease;
    transition-delay: var(--motion-delay, 0ms);
  }

  :global(.motion-enabled [data-motion='image'].is-inview) {
    transition:
      opacity 920ms ease;
  }

  :global(*::selection) {
    background: #ccc;
    color: #fff;
  }
  
  :global(*::-moz-selection) {
    background: #ccc;
    color: #fff;
  }

  /* Custom scrollbar - glassy and skinny */
  :global(::-webkit-scrollbar) {
    width: 6px;
    height: 6px;
  }

  :global(::-webkit-scrollbar-track) {
    background: transparent;
  }

  :global(::-webkit-scrollbar-thumb) {
    background: rgba(0, 0, 0, 0.2);
    border-radius: 10px;
    backdrop-filter: blur(5px);
    -webkit-backdrop-filter: blur(5px);
    transition: background 0.2s ease;
  }

  /* Firefox scrollbar */
  :global(html) {
    scrollbar-width: thin;
    scrollbar-color: rgba(0, 0, 0, 0.2) transparent;
  }

  @media (prefers-reduced-motion: reduce) {
    :global(html) {
      scroll-behavior: auto;
    }

    :global([data-motion]) {
      opacity: 1 !important;
      transform: none !important;
      filter: none !important;
      transition: none !important;
    }
  }
</style>
